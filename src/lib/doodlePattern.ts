// WhatsApp doodle wallpaper — the authentic dense line-art pattern shipped as
// public/whatsapp-bg.png (white doodles on a transparent background), tiled
// behind a chat feed.
//
// Because the doodles are white, they show directly over a dark background.
// Over a light (beige) background, apply the CSS filter `invert(1)` so the
// white lines render as dark doodles instead of washing out.
export const DOODLE_IMG = '/whatsapp-bg.png';
